{
  "name": "zlinks-cc",
  "version": "2.0.0",
  "description": "zlinks.cc personal website",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install",
    "deploy": "npm run deploy -w backend"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
